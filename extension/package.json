{
  "name": "documate-extension",
  "version": "0.0.0",
  "description": "Documate 크롬 확장 — 영어 개발 문서를 그 자리에서 이해하도록 돕는다",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@mozilla/readability": "^0.6.0",
    "preact": "^10.29.7"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.7.1",
    "@preact/preset-vite": "^2.10.5",
    "@types/chrome": "^0.2.2",
    "typescript": "^7.0.2",
    "vite": "^8.1.4"
  }
}
